# Trip Photos

Upload photos into the matching city folder. GitHub Actions will update
`photos.js` automatically after the upload is committed.

Keep filenames simple, for example `chapel-bridge.jpg` instead of names with spaces.
Use `.jpg` where possible; `.HEIC` may not display in every browser.
